get:
  tags:
    - healthCheck
  parameters: []
  operationId: healthCheck-get
  responses:
    '200':
      description: ok
      content:
        application/json:
          schema:
            $ref: '../responses/HealthCheckGetResponse.yaml'
